import { ImageUploadPipe } from './image-upload.pipe';

describe('ImageUploadPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUploadPipe();
    expect(pipe).toBeTruthy();
  });
});
